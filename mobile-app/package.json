{
  "name": "haitian-translator-mobile",
  "version": "1.0.0",
  "description": "English ↔ Spanish ↔ Haitian Creole Translator Mobile App",
  "main": "index.js","scripts": {
    "start": "npx cap serve",
    "build": "npm run copy-web && npx cap sync",
    "copy-web": "mkdir -p www && copy ..\\index.html www\\index.html",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}